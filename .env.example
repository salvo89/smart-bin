# Netlify / GitHub Actions — non commitare valori reali
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:salvatore.bonventre.ai@gmail.com
DISPATCH_SECRET=

# TEST: 1 = push a ogni dispatch (ignora ora e anti-duplicato giornaliero).
# In prod: togliere o mettere 0, e cron workflow su `5 * * * *`.
PUSH_TEST_MODE=1

# Solo GitHub Actions (URL del sito Netlify)
SITE_URL=https://example.netlify.app
